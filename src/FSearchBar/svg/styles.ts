import { style } from "typestyle";

export const SearchIconDiv = style({
	height: "1.5rem",
	overflow: "visible",
	width: "1.5rem",
});

export const ClearIconDiv = style({
	cursor: "pointer",
	height: "1rem",
	overflow: "visible",
	width: "1rem",
});