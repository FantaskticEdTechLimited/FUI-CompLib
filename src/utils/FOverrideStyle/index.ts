/**
 * Overrides the original `className` style of a component.
 *
 * _Use it in_ `className`.
 */
export const FOverrideStyle = (styleParam: string | number) => {
	return styleParam + " !important";
};
