import { stylesheet } from "typestyle";

export const styles = stylesheet({
	headerContainer: {
		display: "flex",
		flexDirection: "row",
		alignItems: "center",
		columnGap: "8px",
		paddingBottom: "2px",
		overflowX: "auto",
		overflowY: "hidden",
	},
});
