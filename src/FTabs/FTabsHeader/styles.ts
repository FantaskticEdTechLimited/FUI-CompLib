import { stylesheet } from "typestyle";

export const styles = stylesheet({
	FTabsHeader_Container: {
		display: "flex",
		flexDirection: "row",
		alignItems: "center",
		columnGap: "8px",
		paddingBottom: "2px",
		overflowX: "auto",
		overflowY: "hidden",
	},
});
