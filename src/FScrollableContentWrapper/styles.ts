import { style } from "typestyle";

export const FScrollableContentWrapper = style({
	borderRadius: 8,
	boxShadow: "inset 0 0 4px rgba(0, 0, 0, 0.12)",
	overflowY: "auto",
	overflowX: "hidden",
	boxSizing: "border-box",
	padding: "16px",
	height: "100%",
});
