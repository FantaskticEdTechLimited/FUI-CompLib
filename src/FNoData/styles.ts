import { stylesheet } from "typestyle";

export const styles = stylesheet({
	FNoData_Container: {
		flexGrow: 1,
		alignItems: "center",
		display: "flex",
		flexDirection: "column",
		rowGap: "24px",
	},
});
