import { style } from "typestyle";

export const fformContainer = style({
    display: "flex",
    flexDirection:"column",
    justifyContent: "flex-start",
    alignItems: "center",
    rowGap:"0.75rem"
}) 