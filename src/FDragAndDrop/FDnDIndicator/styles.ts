import { FColorTypes } from "@innoplus-studio/fui-colorlib";
import { stylesheet } from "typestyle";

export const styles = stylesheet({
	FDnDIndicator_Container: {
		width: "100%",
		backgroundColor: FColorTypes.FThemeColors.MAIN.LM,
		height: "4px",
	},
});
